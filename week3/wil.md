DOM  HTML 태그 하나하나를 자바스크립트로 컨트롤할 수 있도록 만든 트리 구조의 객체 체계입니다. 기존 방식dms 순수 HTML과 JS만으로 여러 명의 개발자가 대규모 코드를 작업하면 코드가 섞이고 유지보수 비용이 기하급수적으로 늘어나는 한계가 있었지만 React를 사용하여 해결할 수 있습니다. JS 코드 안에 HTML을 직접 작성하여 컴포넌트 단위로 나누어 개발할 수 있게 해주는 JSX 문법을 사용한다는걸 알았습니다. SPA (Single Page Application) React를 통해 페이지 새로고침 없이도 한 페이지가 하나의 앱처럼 부드럽게 동작하게 만들 수 있다는걸 알았습니다.

npx create-react-app week3 후 cd week3 로 폴더 진입 npm start로 로컬서버를 실행합니다.